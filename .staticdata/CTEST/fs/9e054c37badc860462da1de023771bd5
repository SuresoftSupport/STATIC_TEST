#pragma once
#include <string>

class Task {
public:
    Task(std::string title, bool done=false);
    void markDone();
    std::string toString() const;
    bool isDone() const;
    std::string getTitle() const;

private:
    std::string title;
    bool done;
};
